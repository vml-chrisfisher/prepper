import initialState from './initialState'
import newsletterReducers from './reducers'
import * as sagas from './sagas'
import * as selectors from './selectors'

export default {
  newsletterReducers,
  initialState,
  sagas,
  selectors,
}
