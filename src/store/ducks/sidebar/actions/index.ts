import initialState from './initialState'
import sidebarActionReducers from './reducers'
import * as selectors from './selectors'

export default {
  sidebarActionReducers,
  initialState,
  selectors,
}
